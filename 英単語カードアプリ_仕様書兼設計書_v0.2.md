# 英単語カード作成アプリ

## 実現可能性評価と仕様書兼設計書

**案1 ｜ v0.2 ｜ 2026年9月18日 ｜ 推奨版と無料OCRを採用**

## 目的と結論

単語帳から英単語と日本語訳を取り込み、手入力の負担を減らすWebアプリを制作する。LaravelとDBを用いるため、提示された上級課題の条件に合う。課題としては、対応レイアウトを限定した読み取り、確認後の一括登録、カード管理、基本的な復習を完成範囲とする推奨案Bを採用する。OCRはNode.js上のTesseract.jsを使い、従量課金APIは使用しない。

カメラ入力と文字認識は既存技術で実現できる。最大の不確実性は、印刷された英単語と訳語を正しく組にする精度である。確認なしの完全自動登録や、任意の単語帳への対応は初期版の完成条件にしない。精度・工数は未実測であり、本書の数値は検証目標または計画上の見積もりである。

## 採用する範囲

| **案** | **内容** | **判断** |
|----|----|----|
| A 短期完成案 | 読み取り、確認、一括保存、CRUD、Anki出力。学習はAnkiで行う。 | 時間が少ない場合の妥協案 |
| B 推奨案 | Aにアプリ内の表裏カード学習、簡易復習、履歴を追加する。 | 採用決定 |
| C 長期目標 | FSRS、双方向カード、音声、同期、柔軟なテンプレートなどを段階的に追加。 | 提出後の拡張 |

## 本書の前提

- 1人で実装する。画面は日本語。スマートフォンで読み取り、PCでも編集・学習できる。

- 当面は印刷された英語と日本語の単語帳1種類を対象とし、利用するページで先に検証する。

- Laravelのバージョンは講義環境を優先する。DBはMySQLを仮置きし、講義環境に合わせて確定する。

- OCRは課金しない方針で確定。提出期限、1日に使える時間、対象端末は引き続き確認する。

読み方：2〜4章は機能仕様、5〜9章は実装設計、10〜12章は検証・計画・根拠、13章は無料OCRの接続仕様。まず1・2・11章で開発範囲を決め、4章の読み取り実験から着手する。

## 1 実現可能性と技術選定

| **要素** | **実現性** | **採用方針と制約** |
|----|----|----|
| 画像から文字を抽出 | 高い | OCRを利用する。実際の単語帳での正確さは別途検証する。 |
| 英単語と訳の対応づけ | 条件付き | 1種類のレイアウトに限定。段組み・例文・欄外注記を除外する。 |
| カメラで読み取る | 高い | 最初は撮影または画像選択。ライブ映像からの1コマ取得は追加実装。 |
| 基本的なカード学習 | 高い | 英語を表示し、訳を開いて自己評価する。 |
| Ankiの全機能と互換 | 範囲が大きい | 提出範囲外。学習履歴の同期やAnki互換スケジューラは作らない。 |

### OCRの選択肢

OCRは画像中の文字をテキスト化する技術であり、そのまま単語カードができるわけではない。文字抽出の後に、単語・訳・品詞を対応づける処理が必要になる。

| **方式** | **利点** | **負担と判断** |
|----|----|----|
| Cloud Vision OCR | 文書内の文字や位置情報を取得できる［S3］。 | 従量課金方式のため今回は採用しない。 |
| Tesseract.js | Node.jsで実行できる無料のOCRライブラリ［S5］。 | 採用。英語と日本語の言語データを使い、手元の単語帳で精度と速度を測る。 |
| 画像対応の生成AI | レイアウトを解釈して項目化する方式を試せる。 | 誤補完や欠落の評価が必要。初期版では採用せず、OCR後処理の拡張候補。 |

### 写真を残さない読み取り

Webカメラの映像から1コマをメモリ上の画像として切り出せば、写真アプリへの保存を必須にしない操作を設計できる。ただし認識処理には画像データが必要で、採用構成ではその画像を自分のLaravelサーバーへ送り、同じ管理環境のNode.jsで処理する。ライブカメラには利用者の許可とHTTPS等の安全な接続が必要になる［S1］。

提出版では撮影または画像選択を入口とし、連続スキャンは後回しにする。capture属性による背面カメラ指定は端末依存のため、ファイル選択も残す［S2］。

### 費用の範囲

OCRライブラリの利用料と文字認識1回ごとのAPI料金はかからない。Node.jsは実行環境であり、文字認識はTesseract.jsが担う。開発時は手元のPCで実行する。公開する場合のサーバー・DB等の費用は別であり、公開運用全体が無料とは限らない。

## 2 機能仕様と完成範囲

Mは短期完成案Aと推奨案Bの必須、Bは推奨案で追加する必須機能、後は提出後の拡張を表す。CRUDは登録・一覧表示・更新・削除のこと。

| **ID** | **優先** | **機能と完了条件** |
|----|----|----|
| F01 | M | ログインとログアウト。各ユーザーは自分のデータだけを扱える。 |
| F02 | M | デッキのCRUD。名前1〜100文字。デッキはカードをまとめる単語帳。 |
| F03 | M | カードのCRUDと検索。英語・訳語を必須とし、品詞・例文・出典は任意。 |
| F04 | M | カメラ撮影または画像選択から、1画像を読み取り処理へ送信する。 |
| F05 | M | 認識した候補を行単位で修正・削除・追加し、保存対象を選べる。 |
| F06 | M | 確認済みの選択行を一括登録する。重複と入力エラーを登録前に表示する。 |
| F07 | M | 選択したデッキをAnki用のタブ区切りテキストとして出力する。 |
| F08 | B | 英語を提示し、操作後に訳を表示する。自己評価を保存する。 |
| F09 | B | 簡易ルールで次回復習日時を決め、期限到来したカードから学習する。 |
| F10 | B | 本日の学習枚数と回答回数、次回復習日時、最近の回答履歴を表示する。 |
| F11 | 後 | 日本語から英語へのカード、音声、タグ、詳細な統計、FSRS対応。 |
| F12 | 後 | リアルタイム連続スキャン、複数レイアウト、Ankiとの自動連携や同期。 |

### カードの入力規則

英語は1〜150文字、日本語訳は1〜1,000文字、品詞は50文字以内、例文は2,000文字以内、出典メモは200文字以内。英語には句動詞・熟語・ハイフン・アポストロフィを許す。単純な英字のみの正規表現で制限しない。前後空白だけを除去し、意味の内容を勝手に変更しない。

### 自動登録の意味

読み取り結果を候補として自動入力し、利用者が確認した後に複数のカードを一度にDBへ登録する。確認前のデータは下書きで、学習対象には含めない。候補が0件でも成功として黙って完了せず、「単語を検出できませんでした」と表示する。

### 初期版で保証しないこと

任意の書籍・手書き・縦書き・複雑な段組みの認識、読み取り漏れの完全検出、発音記号の忠実な再現、Ankiの学習履歴やデッキとの相互同期、オフライン編集は完成条件に含めない。

## 3 画面仕様と利用の流れ

| **画面** | **表示と主な操作** | **次の画面** |
|----|----|----|
| ログイン | メール、パスワード、エラー表示。講義で使う認証方式を再利用する。 | デッキ一覧 |
| デッキ一覧 | デッキ名、カード数、復習対象数、新規作成。 | デッキ詳細 |
| デッキ詳細 | 単語一覧、検索、追加、読み取り、学習、Anki出力。1ページ50件。 | 各機能へ |
| 読み取り | 画像選択、プレビュー、対象範囲の案内、読み取りボタン。 | 確認画面 |
| 読み取り確認 | 元画像と候補一覧。選択、英語、訳、品詞、重複表示。手動行追加。 | 登録結果 |
| カード編集 | 英語、訳、任意項目。保存・削除。未保存で移動する際に通知。 | デッキ詳細 |
| 学習 | 英語、答えを表示、忘れた／覚えていた。表示後だけ回答可能。 | 次のカード |
| 学習状況 | 本日の学習枚数・回答回数、直近20件の履歴。 | デッキ詳細 |

### 読み取りから登録まで

デッキ選択 → 読み取り → 画像プレビュー → OCR処理 → 候補の確認と修正 → 一括登録 → 保存件数の表示、という順に進む。読み取りボタンの連打は無効化し、処理中は進行中であることを表示する。

確認画面では「検出12件・保存予定10件・重複2件」のように件数を表示する。ただし検出件数は元ページの総単語数を保証しない。元画像を見ながら、不足行を利用者が追加できるようにする。

### スマートフォンでの配置

画面上部に画像の開閉プレビュー、その下に1候補ずつ英語と訳を縦に配置する。最下部に保存予定件数と一括登録ボタンを置く。PCでは画像と候補を左右に並べる。タップ対象は概ね44px以上とし、色だけで重複やエラーを伝えない。

### 保存と削除の操作

保存時に入力エラーがあれば該当行へ移動し、修正内容は保持する。カード削除は対象の英単語を、デッキ削除は含まれるカード数を確認画面に表示する。初期版の削除は物理削除で、関連する履歴も削除する。

### 行き止まりを作らない

カメラを利用できない場合は画像選択へ、OCRが失敗した場合は再送または手入力へ案内する。復習対象がない場合は「次の復習日時」を表示し、デッキ詳細へ戻れるようにする。

## 4 読み取り処理の詳細仕様

### 入力条件

初期版はJPEGまたはPNG、1枚5MB以下、最大20メガピクセル、1回50候補以下とする。50件を超えたら切り捨てず、範囲を狭める案内を出す。スマートフォン写真は可能ならブラウザで長辺2,400px程度に縮小する。縮小後の小さい文字の可読性は実測して調整する。HEIC・PDFは対象外とし、形式変更の案内を出す。

### 処理の順序

| **段階** | **処理** |
|----|----|
| 1 受信 | ログイン、デッキ所有者、拡張子と実ファイル形式、容量・画素数を検証する。 |
| 2 OCR | 非公開領域へ一時保存し、内部のNode.js OCRへ画像を送る。OCR上限60秒、Laravelの応答待ちは65秒を暫定値とする。 |
| 3 位置整理 | 取得した文字と座標を、選択範囲内で上から下へ並べる。段組みをまたいで結合しない。 |
| 4 候補化 | 対象レイアウトの規則で見出し語と訳を対応づける。解釈できないものは要確認行にする。 |
| 5 確認 | 候補を下書きとして保存し、利用者が選択・修正する。未検出の単語も追加できる。 |
| 6 確定 | サーバーで再検証し、重複を処理して選択行を一括保存する。元画像を削除する。 |

### 初期の対応レイアウト

共有画像の左ページを初期対象とする。大きな英語見出しと、その下の品詞・日本語訳を抽出し、右側の用例と下部の派生語は除外する。ページ全体に複数の列がある場合は、最初は利用者に1列ずつ切り出してもらう。実際に使う単語帳を確認後、番号・見出し位置・訳の終端・例文の位置を規則として固定する。

英語見出し候補を開始点とし、次の見出し直前までを1ブロックとする。そのブロック内の指定領域から日本語訳を抽出する。単語番号や品詞は分離する。英語例文を見出しに誤認したり、次の単語の訳を連結した場合は失敗として評価する。

### 推測で空欄を埋めない

訳語が読めなければ空欄と「要確認」を表示する。辞書や生成AIで意味を補う機能は別機能として将来検討し、OCRの結果に黙って混ぜない。OCR側の信頼度が得られても、英語と訳の対応が正しいことの保証には使わない。

### 下書きの期限とやり直し

下書きと画像の有効期限は24時間。期限後は再読み取りを求める。OCR失敗時はカードを作らず、同じ画面から再送できる。最初は自動再試行を行わず、CPU負荷と二重処理を避ける。

## 5 システム構成と責務

ブラウザが画面操作と画像選択を担当し、Laravelが認証・OCR連携・検証・DB保存を担当する。文字認識だけを同じ管理環境のNode.jsへ委ねる。画面はLaravelに集約し、Node.jsは外部公開しないOCR専用プロセスとする。

| **構成** | **責務と採用案** |
|----|----|
| 画面 | Blade、Tailwind CSS、必要箇所のJavaScript。カメラ・候補編集のみJSを増やす。 |
| Laravel | セッション認証、CSRF対策、FormRequestで検証、Policyで所有者確認［S8］。 |
| MySQL | ユーザー、デッキ、カード、読み取り下書き、学習履歴を永続化する。 |
| OCRサービス | LaravelのOcrServiceから内部Node.jsへ画像を送る。Tesseract.jsのワーカーを再利用する。 |
| 候補抽出 | VocabularyParserが位置情報を候補へ変換。対象書籍の規則をここへ集約する。 |
| 復習処理 | ReviewServiceが次回日時と履歴を同じDBトランザクションで更新する。 |
| 一時ファイル | 公開ディレクトリ外へ保存。認証済みの専用ルートから本人だけが表示する。 |

### サーバーの前提

PHPと必要な拡張、Composer、Node.js、MySQL、HTTPS、一時保存領域、定期実行環境を用意する。静的HTMLだけを置くホスティングではLaravelとDBは動かない。Node.jsの常駐プロセスを実行できる環境が必要。内部接続用の秘密値は環境変数で管理する。

初期版は単画像の同期処理とし、Node.jsは1ワーカーで1件ずつ実行する。処理中の追加要求は503で返す。Webサーバーの上限は75秒以上とし、実測して調整する。応答時間や同時利用が問題になった段階でキューを導入する。その場合はjob状態の取得、ワーカー運用、失敗時の再実行を追加する。

### エラーの扱い

JSON応答では入力不備422、容量超過413、回数上限429、競合409、内部OCR接続失敗502、処理中または初期化中503、OCRタイムアウト504を使う。認証なしは401、他人の対象は404に統一する。Bladeの通常フォームでは入力不備を元画面へ戻してエラーを表示する。WebセッションのCSRF期限切れはLaravelの419として再ログイン等を案内する。

### ログと設定

記録するのは処理ID、結果、処理時間、件数、エラー種別。元画像・全文OCR結果・秘密情報はアプリログへ残さない。OCRの受付上限は利用者ごとに1分5回・1日50回を暫定値とし、共用キャッシュで管理する。

## 6 データベース設計の基本

MVPは1カードにつき英語→日本語の1方向だけを扱う。Ankiの「ノートから複数カードを作る」仕組みは再現しない。双方向学習を追加する際は、語彙内容と方向別の学習状態を分離する。

| **親** | **子**      | **関係**                                |
|--------|-------------|-----------------------------------------|
| users  | decks       | 1人が複数のデッキを所有する。           |
| decks  | cards       | 1デッキが複数のカードを持つ。           |
| decks  | imports     | 1デッキに複数の読み取り下書きが紐づく。 |
| cards  | review_logs | 1カードに回答のたびに履歴を追加する。   |

### users と decks

| **テーブル** | **主なカラム** | **制約** |
|----|----|----|
| users | id BIGINT、name VARCHAR(100)、email VARCHAR(255)、password VARCHAR(255) | idが主キー、emailが一意。パスワードはハッシュ。 |
| decks | id BIGINT、user_id BIGINT、name VARCHAR(100) | user_idはusersへの外部キー。名前の重複は許可する。 |

### cards

| **カラム** | **型と初期値** | **用途** |
|----|----|----|
| id / deck_id | BIGINT / BIGINT | 主キー / decksへの外部キー |
| export_key | CHAR(36) 一意 | Anki出力用の固定UUID。作成時に生成し変更しない。 |
| term / meaning | VARCHAR(150) / TEXT | 英単語と日本語訳。必須。 |
| part_of_speech | VARCHAR(50) NULL可 | 品詞 |
| example / source | TEXT / VARCHAR(200) NULL可 | 例文と出典メモ |
| content_hash | CHAR(64) | 重複判定用。正規化した英語と訳のSHA-256。 |
| stage | TINYINT 初期0 | 復習の段階。0〜5。 |
| due_at | DATETIME 初期は作成時刻 | 次回復習時刻。UTCで保存する。 |
| review_count | INT 初期0 | 回答回数。0なら新規カード。 |
| lock_version | INT 初期0 | 別タブからの古い更新を拒否するための版番号。 |

全テーブルにcreated_atとupdated_atを置く。cardsはUNIQUE(deck_id, content_hash)、INDEX(deck_id, due_at, id)を追加する。編集・学習更新ごとにlock_versionを1増やす。

## 7 下書きと整合性の設計

### imports

| **カラム** | **型と用途** |
|----|----|
| id / deck_id | BIGINT主キー / decksへの外部キー。所有者はdeck経由で判定。 |
| request_key | CHAR(36) 一意。ブラウザが発行する読み取り要求の識別子。 |
| status | VARCHAR(20)。processing / ready / failed / committed。 |
| image_path / expires_at | VARCHAR(255) NULL可 / DATETIME。非公開画像の場所と期限。 |
| candidates | JSON。行ID、英語、訳、品詞、選択状態、注意事項を保存する。 |
| revision | INT 初期0。候補編集時に増やし、古い画面の上書きを拒否する。 |
| result / error_code | JSON NULL可 / VARCHAR(50) NULL可。保存件数・重複件数等と失敗種別。 |

### review_logs

| **カラム**                 | **型と用途**                                  |
|----------------------------|-----------------------------------------------|
| id / card_id               | BIGINT主キー / cardsへの外部キー。            |
| submission_key             | CHAR(36) 一意。回答の連打や通信再送の識別子。 |
| rating / reviewed_at       | VARCHAR(10) againまたはgood / UTCのDATETIME。 |
| stage_before / stage_after | TINYINT。回答前後の復習段階。                 |
| due_before / due_after     | DATETIME。回答前後の次回復習日時。            |

### 重複と一括登録

英語は前後空白除去・連続空白の圧縮・小文字化、訳は前後空白除去・連続空白の圧縮を行い、区切りを含む配列のJSONからhashを作る。表示内容の大文字小文字は保持する。同一デッキ内で英語と訳の両方が一致すれば初期状態では保存対象から外す。英語が同じでも訳が違えば警告を出し、別カードとして登録できる。

一括確定ではimport行をロックし、所有者・期限・revisionを検証する。選択行をすべて再検証後、同一トランザクション内でカードを作成し、importをcommittedへ変更する。完全一致の重複はスキップし、実保存件数を返す。その他のDBエラーは全件ロールバックする。再送された確定は保存済みresultを返す。

カード編集時もhashを再計算し、完全一致の既存カードがあれば422で差し戻す。ユーザーから送られたuser_idを信用せず、認証ユーザーとdeckの関係を毎回照合する。親削除時は外部キーのcascadeで子行を削除し、画像は別途後処理で削除する。

## 8 ルートと処理インターフェース

同一オリジンのwebルートでセッション認証とCSRFを使う。デッキ・カードのフォームはBlade、読み取りと回答送信はJSONを返す。Laravelのバージョンに合う認証機構を採用し、認証処理は独自実装しない。

| **メソッドとパス**            | **処理**                     | **正常時** |
|-------------------------------|------------------------------|------------|
| GET /decks                    | 自分のデッキ一覧             | 200 HTML   |
| POST /decks                   | デッキ作成                   | 303 詳細へ |
| GET /decks/{deck}             | 一覧・検索・件数表示         | 200 HTML   |
| PATCH /decks/{deck}           | デッキ名更新                 | 303 詳細へ |
| DELETE /decks/{deck}          | デッキと関連データ削除       | 303 一覧へ |
| POST /decks/{deck}/cards      | 手入力のカード作成           | 303 詳細へ |
| PATCH /cards/{card}           | 編集。lock_version必須       | 303 詳細へ |
| DELETE /cards/{card}          | カード削除                   | 303 詳細へ |
| POST /decks/{deck}/imports    | 画像とrequest_keyを受けてOCR | 201 JSON   |
| GET /imports/{import}         | 下書きと候補を取得           | 200 JSON   |
| GET /imports/{import}/image   | 本人の一時画像を表示         | 200 画像   |
| PATCH /imports/{import}       | 候補とrevisionを検証して保存 | 200 JSON   |
| POST /imports/{import}/commit | 最新revisionを指定し一括登録 | 200 JSON   |
| GET /decks/{deck}/study       | 次のカードと回答用トークン   | 200 HTML   |
| POST /cards/{card}/reviews    | 回答とlock_versionを送信     | 201 JSON   |
| GET /decks/{deck}/activity    | 回答履歴と学習件数           | 200 HTML   |
| GET /decks/{deck}/export      | Anki用テキストをダウンロード | 200 添付   |

### 要求と応答の形

候補保存：revisionとitems配列を送る。各行はrow_id、term、meaning、part_of_speech、selectedを持つ。追加行もrow_idを持つ。サーバーは最大50行と文字数を検証し、新しいrevisionを返す。

一括確定：revisionのみを送り、保存済み候補を使う。応答例はsaved_count=10、skipped_count=2、status=committed。登録後に通信が切れても、同じimportを再送すれば同じ結果を返す。

エラーJSONはcode、message、errorsを持つ。行単位の検証エラーはerrors.items.2.meaningのように位置を返す。failedの読み取りをやり直す場合は新しいrequest_keyを使い、同じキーの再送ではOCRを再実行しない。

## 9 学習とAnki出力の仕様

### 簡易復習ルール

AnkiのFSRSとは異なる、課題用の固定段階方式を採用する。学習効果やAnkiと同じ精度は保証しない。日時の計算はサーバーのUTCを基準とし、日本向け画面・日次集計はAsia/Tokyoを使う。

| **回答** | **段階の変更** | **次回日時** |
|----|----|----|
| 忘れた again | stageを0へ戻す | 回答時刻の10分後 |
| 覚えていた good | stageを1増やす。最大5 | 新stage 1 / 2 / 3 / 4 / 5に対し、1 / 3 / 7 / 14 / 30日後 |

対象はdue_atが現在以前のカード。回答済みカードを先にdue_at・id順で出し、その後にreview_count=0の新規カードをid順で出す。新規枚数の1日上限は初期版では設けない。「忘れた」カードは10分後に再び対象となる。期限前の練習モードは提供しない。

回答時はcard行をロックし、所有者、期限到来、lock_versionを確認する。履歴追加とカード更新を同時に確定する。同じsubmission_keyは既存結果を返し、別キーで古いlock_versionを送った場合は409とする。本日の学習枚数はcard_idの重複を除く数、回答回数は履歴行数として区別する。

### Ankiへの出力

UTF-8のタブ区切りテキストを出力する。利用者がファイルを整形する必要はなく、ダウンロード後にAnkiで取り込む。Ankiはテキスト取込と先頭フィールドによる重複判定に対応するため、安定したIDを先頭に置く［S6］。

| **列**     | **内容**                                                       |
|------------|----------------------------------------------------------------|
| 1 ExportID | cards.export_key。編集後も固定。Ankiの内部GUIDには流用しない。 |
| 2 Front    | 英単語                                                         |
| 3 Back     | 訳、品詞、例文、出典を結合したテキスト。空の任意項目は省略。   |

先頭に \#separator:Tab と \#html:false を出す。セル内のタブ・改行は空白へ変換し、二重引用符はCSV規則でエスケープする。通常の列名行は出力しない。初回だけAnkiでExportID・Front・Backの3フィールドを持つノートタイプを作り、表面にFront、裏面にBackを表示する。

再取込は同じノートタイプとIDで更新する。アプリ側の削除や復習日時はAnkiへ同期しない。将来の自動連携は別設計とする。Anki相当の高度な復習が必要なら、まずこの出力経由でAnkiを使う案が現実的である［S7］。

## 10 非機能要件と受入テスト

### 運用とデータ管理

元画像は登録・取消後に削除し、未確定分は24時間で失効させ、1時間ごとの清掃で回収する。したがって物理削除は期限後最大1時間程度遅れ得る。画像配信はprivate, no-storeとし、一時画像はバックアップ対象外にする。認識画像を第三者のOCR APIへ送らない。言語データと実行資材を事前配置してから処理する。

自分のアプリサーバーへ画像を送ることを読み取り画面で説明する。書籍画像やデッキの公開共有機能は設けない。デモでは自作または利用許可のある資料を使用する。DBは提出前にバックアップし、別DBへ復元できることを確認する。

### 受入条件

| **ID** | **確認すること** | **合格条件** |
|----|----|----|
| T01 | CRUDと永続化 | 作成・編集・削除ができ、再ログイン後も正しい内容が残る。 |
| T02 | 所有者の分離 | 別ユーザーのIDを指定しても表示・変更・画像取得・出力ができない。 |
| T03 | 正常な読み取り | 対象レイアウトの鮮明な1ページから候補を作り、一括登録できる。 |
| T04 | 読み取りミス | 空欄、誤訳対応、欠落を確認画面で修正・追加できる。 |
| T05 | 二重送信と重複 | 確定を2回送っても増えない。同じ英語と訳は同一デッキで重複しない。 |
| T06 | 異常系 | 不正画像、5MB超過、Node.js停止、タイムアウトでカードを誤登録しない。 |
| T07 | 復習と時刻 | 2種類の評価、段階上限、JST日付境界、別タブ競合が仕様どおり。 |
| T08 | Anki取込 | 10件を新規取込し、1件の編集後の再取込で重複せず更新される。 |
| T09 | 削除と期限 | デッキ削除・登録完了・期限切れ後に画像を取得できず、清掃で消える。 |
| T10 | 端末と画面 | 対象スマートフォンの縦画面とPCで主要操作が完了する。 |

### 精度と負担軽減の測り方

最初に対象書籍から10ページを選び、正しい英語と訳の組を人手で記録する。5ページで規則を調整し、残り5ページを評価に使う。適合率は正しい組／全候補、再現率は正しく取れた組／元ページの全対象語とする。初期目標は両方90%以上。これは未検証の目標であり、達成できなければ対象範囲を絞る。

20語について手入力時間と「読み取り＋確認修正」の時間を同条件で比較し、30%以上の短縮を目標とする。OCR待ち時間も含める。処理時間の目標は初期化後1画像30秒以内、強制終了上限は60秒とし、CPU・メモリ使用量と実行環境を記録する。未測定の目標値である。

## 11 開発計画と妥協案

最初にOCRと対応づけの小さな実験を行う。その後、手入力だけでカードを保存できる状態を作り、読み取りをつなぐ。下表はLaravelのCRUDを一度学んだ1人が取り組む場合の作業見積もりであり、実測ではない。

| **順序** | **成果物** | **作業時間の目安** |
|----|----|----|
| 1 | 対象ページと端末の確定、Tesseract.jsの実験、精度評価 | 5〜8時間 |
| 2 | 認証、デッキとカードのCRUD、所有者の分離 | 8〜12時間 |
| 3 | 画像入力、一時保存、内部Node.js接続 | 6〜10時間 |
| 4 | 候補抽出、確認編集、一括確定、重複処理 | 12〜20時間 |
| 5 | Anki出力と再取込の検証 | 5〜8時間 |
| 6 | 基本学習、復習日時、履歴の表示 | 6〜10時間 |
| 7 | 異常系・権限・端末テスト、修正 | 8〜12時間 |
| 8 | 動作説明、DB構造の説明、デモ準備 | 4〜6時間 |

推奨案Bは計54〜86時間、調査と手戻りの余裕30%を含め約70〜115時間。1日3時間なら約24〜39作業日が目安になる。学習を並行する場合はさらに延び得る。Aへ絞ると学習機能とその検証を省けるが、OCRの難所は残る。

### 時間が足りないときの削減順

- 第1段階：アプリ内の復習・履歴を外し、Anki出力までを完成させる。

- 第2段階：ライブカメラや自動切り出しを外し、撮影済み画像を1列ずつ選択する。

- 第3段階：単語帳1種類の特定レイアウトに限定し、品詞と例文の自動抽出を外す。

- 最終代替：OCRテキストの貼り付けから候補を作る。課題のDB連携は満たし得るが、カメラ自動読取という当初目的は未達として明示する。

### 作業途中で判断する基準

最初の5〜8時間で、Tesseract.jsの認識と英語・訳の対応が安定するかを確認する。難しければ1列・少数語の読み取りへ縮小する。見積もり上の残り時間の30%を切ったら機能追加を止め、通し操作と修正に使う。公開デプロイが課題条件かは確認し、必要なら環境準備の時間を別に見込む。

### 提出時の説明例

手入力の負担を減らすため、単語帳の画像から英語と訳を候補化し、確認して一括登録できるアプリを制作した。デモでは「読取→修正→DB保存→再表示→学習またはAnki出力」を通して見せ、読み取りの対応範囲と未対応機能も説明する。

## 12 未確定事項と参照資料

### 実装前に確定する項目

| **項目** | **現時点の仮置き** | **確認すると変わる部分** |
|----|----|----|
| 対象単語帳 | 共有画像の左ページを初期検証例にする | 追加ページで候補抽出規則を評価 |
| 対象端末 | スマートフォンとPC | 撮影方法、対応ブラウザ、画像形式 |
| OCR方式 | Node.jsとTesseract.jsで確定 | 言語データ、処理速度、文字認識の設定 |
| 学習場所 | 推奨案Bで確定 | アプリ内の簡易学習とAnki出力を実装 |
| 環境と期限 | 講義Laravel環境を優先。期限未確定 | DB種別、認証、公開方法、実装範囲 |

本書はこの仮置きでも着手できるように作成している。実装済みであることや、読み取り精度が達成済みであることを示す文書ではない。変更時は機能の優先度、DB項目、受入条件を同時に更新する。

### 一次資料

確認日 2026年9月18日。下記のリンクは機能や制約の根拠であり、本書の工数や精度目標は独自の計画値である。

[S1 MDN MediaDevices getUserMedia カメラの許可と安全な接続](https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getUserMedia)

[S2 MDN HTML capture attribute 撮影入力と互換性](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Attributes/capture)

[S3 Google Cloud Vision Detect and extract text OCRの出力](https://docs.cloud.google.com/vision/docs/ocr)

[S4 Google Cloud Vision pricing 画像と機能ごとの料金](https://cloud.google.com/vision/pricing)

[S5 Tesseract.js 開発元リポジトリ](https://github.com/naptha/tesseract.js)

[S6 Anki Manual Text Files テキスト取込と重複更新](https://docs.ankiweb.net/importing/text-files.html)

[S7 Anki Manual Deck Options FSRSと復習設定](https://docs.ankiweb.net/deck-options.html)

[S8 Laravel 12.x Authorization Policyによる認可](https://laravel.com/docs/12.x/authorization)

### 次に行う具体的な作業

共有画像の左ページの0597〜0604を正解8件として、Tesseract.jsの抽出結果と比較する。品詞と訳を確認し、explorationやsimulateが主見出しとして混入しないことも測る。その後、追加ページで規則を検証し、デッキ・カードのマイグレーションを作成する。

## 13 Node.js OCRの接続仕様

v0.2で推奨案Bと無料OCRを採用した。Laravelは画面・認証・DB・単語の組み立てを担当し、Node.jsはTesseract.jsで文字と位置情報を返す。Node.jsだけを導入しても文字認識はできない。

### 内部処理の契約

| **項目** | **仕様** |
|----|----|
| 接続 | Laravelから127.0.0.1の専用ポートへPOST /recognize。Node.jsはループバックに限定して待ち受ける。 |
| 要求 | multipartで検証済みの画像本体と処理IDを送る。ユーザー指定のURLやファイルパスは受け取らない。 |
| 保護 | 内部用トークンをヘッダーで照合する。画像は5MB以下。追加のHTTPヘッダー分を含めて受信上限を設定する。 |
| 応答 | JSONでtextとblocksを返す。文字・座標をLaravel側の共通形式へ変換して候補化する。blocks出力を明示的に有効にする［S9］。 |
| 言語 | 英語engと日本語jpnを使用する。対応する言語データとWASM等を事前に配置し、npm依存をlockfileで固定する。 |
| ワーカー | 起動時に1ワーカーを準備し、画像ごとに作り直さず再利用する。同時実行は1件、処理中の追加要求は503とする［S10］。 |
| 期限 | OCRが60秒を超えたらワーカーを終了・再作成し、504を返す。初期化中は503。Laravelの待ち時間は65秒。 |
| 失敗 | Node.js停止、JSON破損、文字未検出を区別する。いずれも確定カードは作らず、再実行または手入力を案内する。 |

### 画像に合わせた抽出

初期版は利用者が片ページまたは左側の見出しと訳の列を選択する。元画像の番号は欠落確認の補助に使い、必須項目にはしない。文字の高さと位置、単語ごとの区切りを利用し、背景色だけで領域を判定しない。

同じ語の複数の意味は1枚にまとめる。英語見出しの大文字小文字は保持する。必要なら追加カラムとしてsource_number VARCHAR(20) NULL可をcardsへ置き、0597の先頭ゼロを保持する。番号に一意制約は付けない。

### 検証の順序

まず左ページを正面から撮った画像で試す。共有画像の湾曲や傾きへの対応は、その後に評価する。読み取りが不十分なら、無料構成のまま対象範囲の切り出し、解像度、二値化の有無、領域分割を比較する。認識精度はまだ測定していない。

[S9 Tesseract.js API 出力形式と言語データの設定](https://github.com/naptha/tesseract.js/blob/master/docs/api.md)

[S10 Tesseract.js Performance ワーカーとキャッシュの再利用](https://github.com/naptha/tesseract.js/blob/master/docs/performance.md)
